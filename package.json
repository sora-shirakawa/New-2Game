{
  "name": "alchemy-shop-prototype",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "node --check main.js && echo Build successful: no warnings, no errors"
  }
}
